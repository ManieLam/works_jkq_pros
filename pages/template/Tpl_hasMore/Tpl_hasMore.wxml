<!-- 参数 ： data="{{ noMore }}" -->
<template name="hasMore">
	<view class="newCells_ft rel" bindtap="loadMore" wx:if="{{hasMore}}">
		<text class="newText" >查看更多</text>
	</view>
	<!-- <view class="weui-loadmore weui-loadmore_line" wx:if="{{noMore}}">
		<view class="weui-loadmore__tips weui-loadmore__tips_in-line">暂无更多</view>
	</view> -->
	<view class="dividing-line info-fonts" wx:if="{{!hasMore}}"> {{ nohasMore_title || '没有更多了'}} </view>
</template>